table:
  name: votes
  schema: public
insert_permissions:
- permission:
    backend_only: false
    check:
      userId:
        _eq: X-Hasura-User-Id
    columns:
    - inSection
    - voteGroupId
    - voteUserId
    - votingId
    set:
      userId: x-hasura-User-Id
  role: user
select_permissions:
- permission:
    columns:
    - createdAt
    - id
    - sectionId
    - userId
    - votingId
    filter: {}
  role: user

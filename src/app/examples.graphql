query ReferendumVotesBySectionId($sectionId: Int!) {
  referendum_votes(
    where: { sectionId: { _eq: $sectionId } }
    order_by: { questionId: asc }
  ) {
    id
    createdAt
    sectionId
    questionId
    vote
    eVote
  }
}

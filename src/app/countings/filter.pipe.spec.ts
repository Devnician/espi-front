import { CountingsFilterPipe } from './filter.pipe';

describe('CountingsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountingsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
